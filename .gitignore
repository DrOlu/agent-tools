.DS_Store
node_modules/
*.log

# Large binaries (>100 MB) are NOT committed — they ship as release assets on the
# repo's `large-binaries` GitHub release. Keep them out of the tree.
opencode.exe
omp.exe
usql.exe

# Updater scratch / cache
.tmp-*
__pycache__/
